const assert = require('assert');
const { describe, it } = require('node:test');

const { Response } = require('../src/response');

describe('Response', () => {
  describe('send', () => {
    it('sends the content with');
  });
});
